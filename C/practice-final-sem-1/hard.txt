cs300
  HowManyChars
  LovelyVoters
  LuckyDay
  NoisySequence
  SumOfLoves
  Winners


SumOfLoves
  idk how to scanf and ignore spaces
  %[^\n]%*c 

  %[^\n] scan until \n 
    [ ] read character that match the allowed set  
    ^ negation (dont read character in this set)
    
  %*c read but not store the \n 
    %c read a single character   
    * dont store it 

learn string.h

x,y can be char[] or "string" (sometimes)

strcat(x,y) 
  x += y for string 
strcpy(x,y)
  x = y for string 
strcmp(x,y)
  x == y for string 
  compare the ASCII value of x and y at first mismatch 
  the exact number when they dont match depend on compiler 
  if (strcmp(x,y) == 0){
      printf("strings are equal");
    }
  else if (strcmp(x,y) < 0) {
      printf("x < y");
    }
  else if (strcmp(x,y) > 0) {
      printf("x > y");
    }
